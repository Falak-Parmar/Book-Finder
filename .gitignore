# Data
# We allow books.db to be pushed for production deployment
data/vector_store/
data/*.jsonl
data/raw/
data/processed/
*.csv
# Keep sqlite3 and other db patterns ignored, but we will force the main books.db if needed
# Actually, let's just ignore the vector store and raw data specifically

# Python
__pycache__/
*.py[cod]
*$py.class
.env
venv/
.venv/
.DS_Store

analysis/
